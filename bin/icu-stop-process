#!/usr/bin/env lsc
require! program: commander

program
  .arguments '<process-id>'
  .description 'stop a process'
  .action (process-id) ->
    require! forever
    forever.stopbypid process-id, true

program.parse process.argv