#!/usr/bin/env lsc
require! program: commander
ic = require '../'
{control-process} = ic.process!

program
  .version (require require.resolve \../package.json).version
  .description 'turn a process into suspend mode'
  .arguments '<process-name>'
  .action (process-name) ->
    <- control-process process-name, 'pause'

program.parse process.argv
